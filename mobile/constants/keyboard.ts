/** iOS `InputAccessoryView` nativeIDs for estimate steps 1–7 (must match mounted `KeyboardDoneAccessory`). */
export const KEYBOARD_ACCESSORY_IDS = {
  text: 'bps-text-keyboard-accessory',
  number: 'bps-number-keyboard-accessory',
} as const;
